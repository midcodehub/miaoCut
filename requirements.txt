fastapi>=0.110
httpx>=0.27
uvicorn>=0.29
python-multipart>=0.0.9
rembg[cpu]>=2.0.57
# pymatting 本是 rembg 的传递依赖，但 sharp 档的前景色去污染（_decontaminate_foreground）和
# fur legacy 都直接 import 它，所以显式声明，防止 rembg 未来改依赖结构时默认抠图路径突然崩。
pymatting>=1.1.0
pillow>=9.5,<10
slowapi>=0.1.9
# ⚠️ 必须钉死 0.10.18，不能放宽到 <0.11：
#   证件照的人脸关键点走 legacy 的 mp.solutions.face_mesh（见 main.py 的
#   _detect_mediapipe_anchors）。Linux wheel 最新只到 0.10.18（带 solutions），
#   但 macOS ARM 有 0.10.35，那个版本已经移除 solutions 只剩 tasks API。
#   写成范围会让两个平台解析出不同版本：本地 import mp.solutions 直接 AttributeError，
#   被 try 吞掉后静默回退 Haar，线上却是好的 —— 排查时极难发现。
#   将来要升到 0.11+ 必须先把 _detect_mediapipe_anchors 改写成 tasks API（FaceLandmarker）。
mediapipe==0.10.18
numpy>=1.26,<2
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
opencv-python-headless==4.10.0.84
simple-lama-inpainting>=0.1.2
